Algoritmo "exemplo 5 expressao numero quebrado inteiro real"

Var

        a : real
        b : inteiro

Inicio

  a <- 5.3
  b <- a          // nao aceita atribuir conteudo de varialvel real para inteira
  escreval(b)     // entao basta ignora a parte q resta usando o comando
                  // Int(a) na linha 11, ficando b <- Int(a)


Fimalgoritmo