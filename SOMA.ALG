Algoritmo "Soma"

Var
       x, y, soma : inteiro
       


Inicio
        escreva("Valor de X = ")
        leia(x)
        escreva("Valor de Y = ")
        leia(y)
        soma <- (x + y)
        escreval ("Soma = ", soma)
          
Fimalgoritmo